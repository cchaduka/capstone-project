#
# NB:
# These steps have been moved to the Jenkinsfile and are executed in the pipeline itself
#
#lint:
#		docker run --rm -i hadolint/hadolint < Dockerfile
#
#build:
#		sh ./scripts/docker-build.sh
#
#upload:
#		sh ./scripts/docker-upload.sh
